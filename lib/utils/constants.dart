// ignore_for_file: constant_identifier_names

const DESCRIPTION_PREFIX = 'CoTally';
const SUMMARY_PREFIX = "CoTally";

enum Org {
  gitee,
  github,
}

enum Location {
  local,
  remote,
  both,
  none,
}
